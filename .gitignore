# objects produced by compilation
*.o

# executables produced by make build
blockchain
node
miner
client

# things generated at runtime
logs/
fifo/
bootstrap.csv
chain-node-*.csv

# chains saved from the CLI with "save blockchain <name>.csv": they land in
# code/, one level above tests/, so the fixtures stay tracked
code/*.csv
!code/tests/*.csv
# generato localmente, contiene path assoluti
code/compile_commands.json

# configurazione locale degli IDE
.idea/
# configurazione locale degli editor
code/.clangd
